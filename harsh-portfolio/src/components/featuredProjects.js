// Example: Move featured projects data to a separate file for maintainability (for static highlights)
const featuredProjects = [
  // Add static highlights here if needed, or fetch dynamically as in Projects.js
];

export default featuredProjects;
